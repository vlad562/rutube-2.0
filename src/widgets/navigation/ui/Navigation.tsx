import { SidebarItems } from "@/features/sidebar-items/ui/SidebarItems";

const Navigation = () => {
	return (
		<nav className="bg-[#1c232b] w-[300px] min-h-screen">
			<SidebarItems />
		</nav>
	);
};

export default Navigation;
